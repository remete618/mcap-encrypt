{
  "name": "mcap-encrypt",
  "version": "0.1.0",
  "description": "Encrypt and decrypt MCAP files using XChaCha20-Poly1305 + RSA-OAEP",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/ciphers": "^2.0.0",
    "fzstd": "^0.1.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
